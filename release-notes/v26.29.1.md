# v26.29.1

Hotfix. Docs only, no skill or hook behavior changes.

- `fabric-cli`: new `references/service-principals.md` covering service principal setup for automation — creating one with `az ad app create`/`az ad sp create`, the app-id-vs-object-id distinction between `fab auth login` and `fab acl`/group membership, the workspace-role-plus-tenant-setting-group double requirement that silently 401s SP calls when only one is set, real `fab auth login` vs env-token testing (the `FAB_TOKEN` audience gotcha, secret propagation delay, a macOS keychain issue under a HOME override), and secret rotation/teardown.
- README: gallery now includes `paginated-reports`, `custom-visuals`, and `etl`, which were live plugins missing from the plugin list entirely. The `reports` entry drops the four visual-authoring skills that moved to `custom-visuals` in an earlier release and gains the `create-pbi-report` skill it was already shipping. CLI install command list and version badge brought back in sync with the marketplace.
