# v26.31.2

## Repo once-over after the te-cli sync

- `semantic-models/semantic-model`: every `te` example now uses the current syntax that the synced te-cli skill teaches (`-p Name=Value` properties, `te get <obj> --properties`, `te move`, `--model`, `te get . --model` for a summary, whole-model DAX formatting via `te script --inline "Model.AllMeasures.FormatDax();"`). The old `-q <prop> -i <value>`, `te load`, and `te format` forms are gone from the CLI
- `reports/pbir-cli` te-cli tandem: the deploy step uses `--model` / `--target-server` / `--target-database --execute`
- `pbip/pbir-format`: fixed the broken `visual-types/image.md` link in `page.md` (now `images.md`)
